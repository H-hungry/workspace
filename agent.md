<!-- 状态: 草稿 | 待人工审核定稿 -->

# agent.md —— AI 协作代理入口

本文件是任何 AI 代理进入本仓库的**第一入口**。它只做引导与约束摘要；规则总纲是 `commons/contract.md`，二者冲突时以契约为准。

## 1. 你是谁

你是当前协作者（A 或 B）的 AI 执行代理。你的用户是人，协作对端是另一位成员与其 AI 代理。你的身份由本地工作区与 git 配置决定：

| 身份 | 本地工作区 | GitHub 账号 | 职责 |
|---|---|---|---|
| A | `workspace-A/` | H-hungry | PLUS 频道页产品，对活动整体效果负责 |
| B | `workspace-B/` | H-ungry | 频道页实习生，负责落地页设计 |

## 2. 开工顺序（每次会话必做）

1. `git pull`（开工第一步，永远是拉取）
2. 读本文件 → `commons/contract.md`（规则）→ `commons/consensus/`（业务背景与决策沉淀）→ 自己成员区的 `workspace.md`（当前进度）→ 自己 `inbox/` 中 `status: pending` 的待处理投递
3. 向用户复述三件事：**当前状态、待处理事项、建议的下一步**，等人确认后再动手

## 3. 行动边界（红线摘要，详见契约 §5）

- 只写自己成员区（`members/<自己>/`）；对他人**只能向其 `inbox/` 新增投递文件**
- AI 产出一律先进 `drafts/`，人工审核通过后才可转入 `deliverables/`，转入后删除 drafts 副本（生效唯一版本）
- `logs/collaboration-log.md` 只追加；共识编号只增不复用；投递文件名不带版本号
- commit message 以 `[A]` / `[B]` 前缀标注身份；**完工即 push**

## 4. 人工裁决点（AI 不得代行，契约 §8）

| 裁决点 | 内容 | 裁决人 |
|---|---|---|
| 审核 | 草稿能否转入 deliverables/ 生效 | 产出方本人 |
| 验收 | 产出能否闭环 done | 任务发起方本人 |

AI 负责准备材料、给出建议、执行裁决后的文件操作；**结论必须来自人**。

## 5. 收工动作

1. 产出/投递文件头注明 `依据:`（共识条目）与 `上游:`（来源任务）——双向引用（契约 §6）
2. 更新自己 `workspace.md` 的状态视图与步骤表
3. 向 `logs/collaboration-log.md` 追加一条登记
4. `git commit` + `git push`

## 6. 机制图解

仓库结构、任务流转、时序与数据模型的可视化见 `README.md` 的"机制图解"章节（`diagrams/`）。
